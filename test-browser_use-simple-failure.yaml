name: "Browser_use YouTube Failure Test"
description: "Test browser_use failure detection with realistic YouTube search task"

tests:
  - name: "Search for non-existent video on YouTube"
    cleanup:
      always:
        - name: "cleanup browser session"
          plugin: playwright
          config:
            role: stop
            session_id: "youtube-fail-{{ .run.id }}"
    steps:
      - name: "start browser"
        plugin: playwright
        config:
          role: start
          session_id: "youtube-fail-{{ .run.id }}"
          headless: false

      - name: "navigate to YouTube"
        plugin: playwright
        config:
          role: script
          session_id: "youtube-fail-{{ .run.id }}"
          language: python
          script: |
            from playwright.sync_api import expect

            print("🎯 Navigating to YouTube...")
            page.goto("https://www.youtube.com")
            expect(page).to_have_url("https://www.youtube.com/")
            print("✅ YouTube loaded")

            result = {"status": "ready"}

      - name: "search for specific non-existent video"
        plugin: browser_use
        config:
          session_id: "youtube-fail-{{ .run.id }}"
          task: |
            Find a youtube comment that says "Hi my name is Phil" and click on it.
          max_steps: 2
          use_vision: true
          llm:
            provider: "openai"
            model: "gpt-4o"
            config:
              OPENAI_API_KEY: "{{ .env.OPENAI_API_KEY }}"
