version: "v1.0.0"
name: "Basic Shell Test"
description: "Simple test to verify shell executor works"

tests:
  - name: "Hello World Test"
    steps:
      - name: "Simple echo"
        plugin: script
        config:
          language: shell
          script: echo "Hello from Rocketship shell executor!"

  - name: "Variable Test"
    steps:
      - name: "Config variable test"
        plugin: script
        config:
          language: shell
          script: |
            echo "Testing shell executor"
            echo "All working correctly"