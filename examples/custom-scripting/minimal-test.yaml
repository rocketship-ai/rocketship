name: "Minimal Test"
version: "v1.0.0"
vars:
  test: "works"

tests:
  - name: "Just Script"
    steps:
      - name: "Script only"
        plugin: "script"
        config:
          language: "javascript"
          script: |
            save('result', vars ? 'vars_exists' : 'vars_missing');