steps:
  - label: ":go: unit-tests"
    commands:
      - make build

  - label: ":rocket: integration"
    commands:
      - rocketship start --ci
      - rocketship run --file examples/order-workflow/rocketship.yaml --format junit --output rs-results.xml
    artifact_paths: "rs-results.xml"
