# Minikube Development Secrets
# Copy this file to .env and fill in with production values from DO cluster
# See docs/src/deploy/minikube.md for detailed setup instructions

# =============================================================================
# REQUIRED SECRETS (must be provided by you)
# =============================================================================

# GitHub OAuth (for user authentication)
# Create a GitHub OAuth App at https://github.com/settings/developers
# IMPORTANT: Enable "Device Flow" in the OAuth App settings
# Set callback URL to: http://auth.minikube.local/callback
ROCKETSHIP_GITHUB_CLIENT_ID=your-github-oauth-client-id
ROCKETSHIP_GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# Email (Postmark)
ROCKETSHIP_EMAIL_FROM=your-email-from-address
ROCKETSHIP_POSTMARK_SERVER_TOKEN=your-postmark-server-token

# Database (optional, auto-generated if not set)
POSTGRES_PASSWORD=your-postgres-password

# =============================================================================
# GITHUB APP (for repository access - optional but recommended)
# =============================================================================

# GitHub App credentials (for repo content access)
ROCKETSHIP_GITHUB_APP_ID=your-github-app-id
ROCKETSHIP_GITHUB_APP_SLUG=your-github-app-slug
ROCKETSHIP_GITHUB_APP_PRIVATE_KEY_FILE=/path/to/private-key.pem

# =============================================================================
# GITHUB WEBHOOKS (for auto-sync via smee.io relay - optional)
# =============================================================================

# GitHub Webhook (for webhook ingestion via smee.io relay)
ROCKETSHIP_GITHUB_WEBHOOK_SECRET=your-webhook-secret
ROCKETSHIP_GITHUB_WEBHOOK_SMEE_URL=https://smee.io/your-channel

# =============================================================================
# AUTO-GENERATED SECRETS (created by setup-local-dev.sh if not present)
# These are appended to .env automatically on first run
# =============================================================================

# Refresh token HMAC key (32 bytes, base64 encoded)
# ROCKETSHIP_CONTROLPLANE_REFRESH_KEY=<auto-generated>

# Signing key file path (RSA private key for JWT signing)
# ROCKETSHIP_CONTROLPLANE_SIGNING_KEY_FILE=.secrets/controlplane-signing-key.pem
