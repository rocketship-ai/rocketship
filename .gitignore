.vscode
/bin/
/internal/embedded/bin/*
!/internal/embedded/bin/.gitkeep

# MCP server Node.js dependencies
mcp-server/node_modules/
mcp-server/npm-debug.log*
mcp-server/yarn-debug.log*
mcp-server/yarn-error.log*

# python
.DS_Store
**/.DS_Store

# documentation build artifacts
docs/site/
docs/.venv/

# working with teams
honeyruns/

# Temporary build artifacts
tmp/
cli-introspection.json
*.log

# user testing
**/.env
.rocketship/
.playwright-mcp/

# GitHub App private keys (local dev only; never commit)
*.private-key.pem
*.private-key*.pem

# MCP server temporary artifacts (screenshots, etc.)
# MCP servers write to .rocketship/tmp/mcp-<server-name>/
.rocketship/tmp/

# rocketship-internal code
charts/rocketship-internal/

# Plans
plans/

#symlinks
rocketship-internal
chickenfarm

# Local secrets vault (NEVER commit)
rocketship-vault/

# Local secrets directory for development
.secrets/
