name: Pull Request Check (Legacy - See Individual Workflows)

on:
  pull_request:
    branches: ["main"]

jobs:
  redirect-info:
    name: CI Workflow Information
    runs-on: ubuntu-latest
    steps:
      - name: CI Workflow Reorganization Notice
        run: |
          echo "🔄 The CI workflows have been reorganized into focused, individual files:"
          echo ""
          echo "📦 Build & Test:"
          echo "   File: .github/workflows/build-test.yml"
          echo "   Tests: Go builds, cross-platform compilation, Docker builds, linting"
          echo ""
          echo "🖥️  CLI Integration:"
          echo "   File: .github/workflows/cli-integration.yml"
          echo "   Tests: CLI commands, examples, database integration, browser plugin"
          echo ""
          echo "📚 Documentation:"
          echo "   File: .github/workflows/docs-validation.yml"
          echo "   Tests: Plugin docs sync, schema validation"
          echo ""
          echo "🔌 MCP Integration:"
          echo "   File: .github/workflows/mcp-integration.yml"
          echo "   Tests: MCP server functionality, npm package"
          echo ""
          echo "⚓ Helm Charts:"
          echo "   File: .github/workflows/helm-chart-tests.yml"
          echo "   Tests: Helm chart deployment, validation, upgrade/uninstall"
          echo ""
          echo "🎯 Orchestrator:"
          echo "   File: .github/workflows/pr-check-orchestrator.yml"
          echo "   Purpose: Provides overview of all workflow statuses"
          echo ""
          echo "✅ This reorganization provides:"
          echo "   - Better parallelization (all workflows run simultaneously)"
          echo "   - Comprehensive testing (all tests run on every PR)"
          echo "   - Easier debugging and maintenance"
          echo "   - Focused, manageable workflow files"
          echo ""
          echo "Check the 'Actions' tab to see all individual workflows running!"